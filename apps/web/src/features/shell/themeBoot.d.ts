/** Declaration for the plain-JS `themeBoot.js`, which is JS rather than TS so
 * the production server can import the identical string to hash for its CSP
 * without going through the TypeScript build. */
export declare const themeBootScript: string;
